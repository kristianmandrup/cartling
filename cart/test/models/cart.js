'use strict';

var should = require('should');
var _ = require('lodash');

var model = require('../helpers').models;
var Cart = model.Cart;

describe('Cart Model', function() {

  this.timeout(10000);

});
